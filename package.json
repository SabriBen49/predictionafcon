{
  "name": "jsonserver",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch prediction.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
